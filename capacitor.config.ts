import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.typingrap.battle',
  appName: 'Typing Rap Battle',
  webDir: 'public'
};

export default config;
